[build]
  command = "npm run build"
  publish = "dist"

# Single-page app: always serve index.html (safe if you add client-side routes later)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
